# 05 - 分享功能技術規格

> **目標**：讓使用者能輕鬆分享 AI 摘要，並透過口碑傳播帶動新用戶註冊。

---

## 🎯 業務目標

```
使用者分享連結 → 被分享者閱讀摘要 → 發現 TubeSummary → 註冊使用
```

---

## 📦 功能模組

| 模組 | 優先級 | 狀態 |
|------|--------|------|
| 加強 Video 公開頁面 OG meta | P0 | ✅ 完成 |
| 新增 Episode 公開頁面 | P0 | ✅ 完成 |
| ShareButton 元件 | P0 | ✅ 完成 |
| ArticleModal 整合分享按鈕 | P0 | ✅ 完成 |
| FeedCard 整合分享按鈕 | P0 | ✅ 完成 |
| RSS Feed 加入分享連結 | P1 | ✅ 完成 |
| CTA Banner 元件 | P1 | ✅ 完成 |

---

## 🌐 i18n 翻譯鍵值（必須先新增）

> ⚠️ **重要**：根據 `99_lessons_learned.md`，所有 user-facing text 必須使用 i18n，不可硬編碼。

**檔案**: `messages/en.json` 和 `messages/zh-TW.json`

```json
// messages/en.json
{
  "Share": {
    "copy_link": "Share",
    "copied": "Copied!",
    "copy_failed": "Copy failed",
    "cta_title": "This summary was generated by {brand} AI",
    "cta_subtitle": "Want automatic high-quality summaries of your subscriptions?",
    "cta_button": "Try Free"
  }
}

// messages/zh-TW.json
{
  "Share": {
    "copy_link": "分享",
    "copied": "已複製！",
    "copy_failed": "複製失敗",
    "cta_title": "這篇摘要由 {brand} AI 自動產生",
    "cta_subtitle": "想自動收到你訂閱頻道的高品質摘要？",
    "cta_button": "免費試用"
  }
}
```

---

## 🔧 共用工具函式

在實作前，先將 `stripHtml` 移至共用位置（目前在 `FeedCard.tsx` 中有重複定義）：

**檔案**: `lib/utils.ts`

```ts
export function stripHtml(html: string): string {
    return html.replace(/<[^>]*>/g, '').replace(/&nbsp;/g, ' ').trim();
}
```

---

## 🏗️ 技術實作細節

### 1. Video 公開頁面 OG Meta 加強

**檔案**: `app/[locale]/video/[videoId]/page.tsx`

現有頁面已存在，需加強 `generateMetadata` 函式：

```tsx
export async function generateMetadata({ params }: PageProps): Promise<Metadata> {
    const { videoId } = await params;
    const video = await prisma.youtubeVideo.findUnique({
        where: { youtube_video_id: videoId },
        include: { channel: true, summaries: true },
    });

    if (!video) return { title: 'Video Not Found' };

    const summary = video.summaries[0];
    const description = summary 
        ? stripHtml(summary.content).slice(0, 160) 
        : `AI 摘要：${video.title}`;

    return {
        title: `${video.title} | AI 摘要`,
        description,
        openGraph: {
            title: video.title,
            description,
            type: 'article',
            siteName: 'TubeSummary',
            images: [`https://i.ytimg.com/vi/${videoId}/maxresdefault.jpg`],
        },
        twitter: {
            card: 'summary_large_image',
            title: video.title,
            description,
            images: [`https://i.ytimg.com/vi/${videoId}/maxresdefault.jpg`],
        },
    };
}
```

---

### 2. Episode 公開頁面（新增）

**檔案**: `app/[locale]/episode/[episodeId]/page.tsx`

結構與 video 頁面相同，主要差異：

- 使用 `prisma.podcastEpisode.findUnique({ where: { id: episodeId } })`
- OG image 使用 `episode.podcast.image_url`
- Embed 改為 `<audio>` 元素

---

### 3. ShareButton 元件

**檔案**: `components/ui/ShareButton.tsx`

```tsx
'use client';

import { Share2, Check, X } from 'lucide-react';
import { useState } from 'react';
import { useTranslations } from 'next-intl';
import { IconButton } from './IconButton';

interface ShareButtonProps {
    url: string;
    className?: string;
}

type CopyState = 'idle' | 'copied' | 'failed';

export function ShareButton({ url, className }: ShareButtonProps) {
    const t = useTranslations('Share');
    const [copyState, setCopyState] = useState<CopyState>('idle');

    const handleShare = async (e: React.MouseEvent) => {
        e.stopPropagation(); // 防止觸發卡片點擊

        const fullUrl = `${window.location.origin}${url}`;

        try {
            await navigator.clipboard.writeText(fullUrl);
            setCopyState('copied');
        } catch {
            // Fallback for older browsers or restricted contexts
            try {
                const textArea = document.createElement('textarea');
                textArea.value = fullUrl;
                textArea.style.position = 'fixed';
                textArea.style.opacity = '0';
                document.body.appendChild(textArea);
                textArea.select();
                document.execCommand('copy');
                document.body.removeChild(textArea);
                setCopyState('copied');
            } catch {
                setCopyState('failed');
            }
        }

        setTimeout(() => setCopyState('idle'), 2000);
    };

    const icon = {
        idle: <Share2 className="w-5 h-5" />,
        copied: <Check className="w-5 h-5 text-green-500" />,
        failed: <X className="w-5 h-5 text-red-500" />,
    }[copyState];

    const label = {
        idle: t('copy_link'),
        copied: t('copied'),
        failed: t('copy_failed'),
    }[copyState];

    return (
        <IconButton
            variant="default"
            aria-label={label}
            title={label}
            onClick={handleShare}
            className={className}
        >
            {icon}
        </IconButton>
    );
}
```

---

### 4. ArticleModal 整合

**檔案**: `components/feed/ArticleModal.tsx`

在 header 區域（第 40-52 行）加入 ShareButton：

```tsx
import { ShareButton } from '@/components/ui/ShareButton';

// 在 <div className="flex items-center gap-2"> 內新增：
<ShareButton 
    url={article.type === 'video' 
        ? `/video/${article.youtubeVideoId}` 
        : `/episode/${article.id}`
    } 
/>
```

---

### 5. FeedCard 整合

**檔案**: `components/feed/FeedCard.tsx`

> ⚠️ **重要**：根據 `lib/types.ts` 的 `FeedItem` 定義：
> - `id` = 內部 DB ID（數字轉字串）
> - `youtubeVideoId` = YouTube 影片 ID（如 `dQw4w9WgXcQ`）
>
> Video 公開頁面 URL 格式為 `/video/[youtubeVideoId]`，因此 **必須** 新增 prop。

**Step 1**: 更新 `FeedCardProps` interface：

```tsx
interface FeedCardProps {
    type: 'video' | 'episode';
    id: string;
    youtubeVideoId?: string;  // 新增：video 類型必須提供
    title: string;
    // ... 其他 props
}
```

**Step 2**: 在 thumbnail 區域加入 hover 顯示的分享按鈕：

```tsx
import { ShareButton } from '@/components/ui/ShareButton';

// 在 FeedCard 函式參數中解構 youtubeVideoId
export function FeedCard({ type, id, youtubeVideoId, title, ... }: FeedCardProps) {

// 在 thumbnail div 內，Badge 之後新增：
<div className="absolute top-1 right-1 opacity-0 group-hover:opacity-100 transition-opacity">
    <ShareButton
        url={type === 'video' ? `/video/${youtubeVideoId}` : `/episode/${id}`}
    />
</div>
```

**Step 3**: 更新所有使用 `FeedCard` 的地方，傳入 `youtubeVideoId` prop。

---

### 6. RSS Feed 分享連結

**檔案**:
- `app/[locale]/feed/user/[token]/route.ts`
- `app/[locale]/feed/user/[token]/channel/[channelId]/route.ts`
- `app/[locale]/feed/user/[token]/podcast/[podcastId]/route.ts`

> ⚠️ **注意**：現有 RSS 路由使用 CDATA 包裝 description，分享連結必須插入 CDATA **內部**。

**Step 1**: 新增 helper function（可放在檔案頂部或 `lib/rss-utils.ts`）：

```tsx
const shareFooter = (shareUrl: string, baseUrl: string) => `
    <hr style="margin: 20px 0; border-color: #333;"/>
    <p style="color: #888; font-size: 14px;">
        🔗 Share this summary: <a href="${shareUrl}">${shareUrl}</a><br/>
        Generated by <a href="${baseUrl}">TubeSummary</a> AI
    </p>
`;
```

**Step 2**: 修改現有的 RSS XML 模板，在 description CDATA 內加入 footer：

```tsx
// 原本：
<description><![CDATA[
    <p><strong>From: ${item.source}</strong></p>
    ${item.description}
]]></description>

// 修改為：
<description><![CDATA[
    <p><strong>From: ${item.source}</strong></p>
    ${item.description}
    ${shareFooter(
        item.type === 'video'
            ? `${baseUrl}/video/${item.youtubeVideoId}`
            : `${baseUrl}/episode/${item.episodeId}`,
        baseUrl
    )}
]]></description>
```

**Step 3**: 確保 `allItems` map 包含 `youtubeVideoId` 和 `episodeId`：

```tsx
...videoItems.map(v => ({
    type: 'video' as const,
    youtubeVideoId: v.youtube_video_id,  // 新增
    // ...
})),
...episodeItems.map(e => ({
    type: 'podcast' as const,
    episodeId: e.id,  // 新增
    // ...
})),
```

---

### 7. CTA Banner 元件

**檔案**: `components/ui/CTABanner.tsx`

> ⚠️ **注意**：此元件用於公開頁面，無法使用 `useTranslations` hook（Server Component）。
> 需使用 `getTranslations` 或由父元件傳入翻譯好的字串。

**方案 A**：作為 Server Component（推薦）

```tsx
import Link from 'next/link';
import { getTranslations } from 'next-intl/server';
import { Button } from './Button';

export async function CTABanner() {
    const t = await getTranslations('Share');

    return (
        <div className="mt-8 p-6 bg-gradient-to-r from-orange-500/10 to-amber-500/10
                        rounded-xl border border-orange-500/20 text-center">
            <p className="text-lg mb-4">
                {t.rich('cta_title', {
                    brand: (chunks) => <strong className="text-orange-400">{chunks}</strong>
                })}
            </p>
            <p className="text-muted-foreground mb-4">
                {t('cta_subtitle')}
            </p>
            <Link href="/">
                <Button variant="primary">{t('cta_button')}</Button>
            </Link>
        </div>
    );
}
```

**方案 B**：接收 props（若需要 Client Component）

```tsx
interface CTABannerProps {
    title: string;
    subtitle: string;
    buttonText: string;
}

export function CTABanner({ title, subtitle, buttonText }: CTABannerProps) {
    // ...
}
```

放置於 `video/[videoId]/page.tsx` 和 `episode/[episodeId]/page.tsx` 的 footer 區域（`</footer>` 之前）。

---

## ✅ 驗收標準

1. **Open Graph 預覽**
   - 將摘要連結貼到 LINE/Slack/Twitter
   - 應顯示：標題、前 160 字描述、影片縮圖
   - 使用 [Facebook Debugger](https://developers.facebook.com/tools/debug/) 驗證

2. **分享 UX**
   - ArticleModal 內點擊分享 → 顯示「已複製」圖示
   - FeedCard hover 可見分享按鈕
   - 複製失敗時顯示錯誤狀態（紅色 X）

3. **RSS 驗證**
   - 使用 RSS reader 訂閱 → 每則內容底部有分享連結
   - 連結格式正確（`/video/{youtubeVideoId}` 或 `/episode/{id}`）

4. **CTA 轉換**
   - 訪問 `/video/{youtubeVideoId}` → 頁尾可見 CTA → 點擊導向首頁

5. **i18n 驗證**
   - 切換語言（en ↔ zh-TW）後所有文字正確顯示
   - 無 hardcoded 中文或英文字串

---

## 📋 實作前檢查清單

在開始實作前，確認以下項目：

- [ ] 新增 i18n 鍵值至 `messages/en.json` 和 `messages/zh-TW.json`
- [ ] 將 `stripHtml` 函式移至 `lib/utils.ts`（避免重複定義）
- [ ] 確認 `IconButton` 元件支援 `variant`、`onClick`、`title` props
- [ ] 確認 `FeedItem` 介面已有 `youtubeVideoId` 欄位
- [ ] 確認所有 3 個 RSS 路由都需要更新

---

## 📁 相關檔案快速導覽

| 類型 | 路徑 |
|------|------|
| Video 公開頁 | `app/[locale]/video/[videoId]/page.tsx` |
| Episode 公開頁 | `app/[locale]/episode/[episodeId]/page.tsx` (新增) |
| 摘要彈窗 | `components/feed/ArticleModal.tsx` |
| 資訊流卡片 | `components/feed/FeedCard.tsx` |
| RSS 路由 | `app/[locale]/feed/user/[token]/*/route.ts` |
| 類型定義 | `lib/types.ts` (`FeedItem`) |
| i18n 翻譯 | `messages/en.json`, `messages/zh-TW.json` |
| 共用工具 | `lib/utils.ts` (`stripHtml`) |
